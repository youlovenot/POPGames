<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberDAO">

	<resultMap id="memberResult" type="member">
		<id property="id" column="ID"/>
		<result property="nickname" column="NICKNAME"/>
		<result property="content" column="CONTENT"/>
		<result property="regDate" column="REGDATE"/>
		<result property="cnt" column="CNT"/>
	</resultMap>

	<insert id="insertMember">
			INSERT INTO MEMBER(ID, PASSWORD, NAME, PHONE, EMAIL, ZIPCODE, ADRESS1, ADRESS2)
			VALUES(#{id},#{password},#{name},#{phone},#{email},#{zipcode},#{adress1},#{adress2})
	</insert>
	
	<update id="updateMember">
			UPDATE MEMBER SET
			PASSWORD=#{password}, PHONE=#{phone]}, EMAIL=#{email}, NAME=#{name}
			ZIPCODE=#{zipcode}, ADRESS1=#{address1}, ADRESS2=#{adress2}
			where ID=#{id}
	</update>
	
	<delete id="deleteMember">
			DELETE MEMBER WHERE ID=#{id}
	</delete>
	
	<select id="getMember" resultType="member">
			SELECT * FORM MEMBER WHERE ID=#{id}
	</select>
	
</mapper>